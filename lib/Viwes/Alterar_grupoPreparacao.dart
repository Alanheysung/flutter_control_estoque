import 'package:flutter/material.dart';

class AlterarGrupoScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Tela Alterar Grupo")),
      body: Center(
        child: Text("Tela para Alterar Grupo"),
      ),
    );
  }
}
