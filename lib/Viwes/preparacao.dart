import 'package:flutter/material.dart';

class Preparacao extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Tela Preparação")),
      body: Center(
        child: Text("Aqui está a Preparação!"),
      ),
    );
  }
}
