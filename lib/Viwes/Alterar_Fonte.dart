import 'package:flutter/material.dart';

class AlterarFonte extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Alterar Fonte')),
      body: Center(
        child: Container(), // Corpo vazio
      ),
    );
  }
}


