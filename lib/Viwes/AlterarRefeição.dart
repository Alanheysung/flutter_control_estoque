import 'package:flutter/material.dart';

class Alterrefeicao extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Alterar Grupo')),
      body: Center(
        child: Text(
          'Bem-vindo à tela de Alterar Grupo!',
          style: TextStyle(fontSize: 20),
        ),
      ),
    );
  }
}